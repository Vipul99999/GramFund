import { FastifyReply, FastifyRequest } from 'fastify';

export async function transactionMiddleware(_request: FastifyRequest, _reply: FastifyReply) {
  // Placeholder for Prisma.$transaction wrapper.
}
